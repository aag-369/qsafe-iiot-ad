# Developer dependencies for working on the Q-Safe Field Link demo locally.
# The DEPLOYED service installs demo/requirements-runtime.txt instead -- see
# that file for why the split exists.

-r requirements-runtime.txt

fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
matplotlib>=3.8      # figures in the recorded evidence pack (local only)
